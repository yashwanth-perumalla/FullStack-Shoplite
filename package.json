{
  "name": "shoplite",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "cross-env NEXT_PUBLIC_API_URL=http://localhost:5002 npm --prefix frontend run dev",
    "start": "concurrently -n API,WEB -c blue,green \"npm:start:api\" \"npm:start:web\"",
    "start:api": "npm --prefix backend run start",
    "start:web": "cross-env NEXT_PUBLIC_API_URL=http://localhost:5002 npm --prefix frontend run start"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "cross-env": "7.0.3"
  }
}
